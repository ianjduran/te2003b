/*
 * poll_keyboard_task.c
 *
 *  Created on: May 30, 2022
 *      Author: Ian Duran
 */

#include "poll_keyboard_task.h"


extern QueueHandle_t xQueue;
const TickType_t xTicksToWait = pdMS_TO_TICKS( 200 );

void poll_keyboard_button_task(uint8_t row, uint8_t col){
	/* FAVOR DE IGNORAR, NI SE SI ESTA BIEN*/
//	for(int i = 4; i < 8; i++){
//		GPIOA->ODR |= 1 << i;
//	}
//	GPIOA->ODR &= ~(1 << row);
//	(GPIOA->IDR & (1 << col))
//	// Read col

	xQueueReceive(xQueue, (uint8_t) 5, xTicksToWait);
}
